version: '3.2'

services:
  kea:
    build: .
    env_file: /data/credentials/proforma/.secure_info
    network_mode: "host"
    container_name: kea
    volumes:
      - type: bind
        source: ./src/test_data
        target: /usr/src/app/data
        read_only: true